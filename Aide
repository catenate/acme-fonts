/fonts/Aide
Dump /github/acme-fonts/dump
Edit ,|sort|uniq
Man 6 font
git
git add .
git commit -a -m 'Don''t need Edit in front of commands, since we can highlight text in a window, highlight a command, and 2-1 to apply it.'
git fetch
git log --graph --oneline --all
git push origin master
log 1 git
ls -F | mc
test/1/acme/Font/fonts
